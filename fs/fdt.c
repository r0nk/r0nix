#include "ext2/ext2.h"

int get_fd(ext2_inode inode){
	/* find the file in the fdt*/
	/* if its in the fdt*/
		/* return the one thats already there*/
	/* find a free spot in the fdt */
	/* make a fd and put it there */
	/* return its index*/
}
